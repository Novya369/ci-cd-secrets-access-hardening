{
  "rules": [
    {
      "id": "no-hardcoded-secrets",
      "description": "Pipelines must not contain hardcoded secrets"
    },
    {
      "id": "least-privilege-permissions",
      "description": "Pipelines must define minimal required permissions"
    }
  ]
}
