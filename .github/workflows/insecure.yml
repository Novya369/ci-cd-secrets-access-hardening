name: Insecure CI Pipeline

on: [push]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Use hardcoded secret (BAD PRACTICE)
        run: |
          echo "Using API_KEY=hardcoded-secret-value"
